export const unauthenticatedOnlyRoutes = ["/", "/login", "/register"];

export const authenticatedOnlyRoutesPrefixes = ["/home", "/settings"];

export const apiAuthPrefix = ["/api/auth"];

export const DEFAULT_AUTHENTICATED_REDIRECT = "/home";

export const DEFAULT_UNAUTHENTICATED_REDIRECT = "/login";

export const DEFAULT_LOGOUT_REDIRECT = "/";
